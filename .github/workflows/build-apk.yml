name: Build Jelly APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Build Jelly APK
        run: |
          PROJECT=.
          if [ -f jelly22/settings.gradle ]; then
            PROJECT=jelly22
          fi
          gradle --no-daemon -p "$PROJECT" :app:assembleDebug

          mkdir -p dist
          cp "$PROJECT/app/build/outputs/apk/debug/app-debug.apk" dist/JellyMarket-3.0-complete.apk
          sha256sum dist/JellyMarket-3.0-complete.apk > dist/JellyMarket-3.0-complete.apk.sha256

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: JellyMarket-3.0-complete-debug
          path: |
            dist/JellyMarket-3.0-complete.apk
            dist/JellyMarket-3.0-complete.apk.sha256
          if-no-files-found: error
